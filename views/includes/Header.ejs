<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <header>
        <nav
            class="flex justify-between font-semibold pl-7 pr-7 mt-5 items-center bg-green-500 h-15 w-[85vw] m-auto rounded-full">
            <ul class="flex gap-3">
                <li><a href="/products">Products</a></li>
                <li><a href="/cart">Cart</a></li>
                <li><a href="/orders">Orders</a></li>
                <li><a href="/admin/addProduct">Add products</a></li>
                <li><a href="/admin/products">Admin products</a></li>
            </ul>
            <div class="flex items-center justify-center gap-3">
                <% if(email) { %>
                    <div class="p-1 bg-black text-white rounded-md">Signed in as <%= email %></div>
                <% } %>
                <a href="/signup">Signup</a>
                <a href="/<% if(isAuthenticated) { %>signout<% } else { %>login<% } %>">
                    <% if(isAuthenticated) { %>Logout<% } else { %>Login<% } %>
                </a>
            </div>
        </nav>
    </header>